---
title: "Deposits"
openapi: "GET /api/bridge/deposit"
description: "Fund your trading wallets on Polymarket and Kalshi"
---

## Overview

There are two ways to deposit funds depending on the venue:

<CardGroup cols={2}>
  <Card title="Polymarket" icon="bridge">
    Use the Bridge API to deposit from any chain. Funds are automatically converted to USDC.e on Polygon.
  </Card>
  <Card title="Kalshi" icon="arrow-right">
    Send USDC and SOL directly to your `kalshiWalletAddress` on Solana.
  </Card>
</CardGroup>

## Kalshi Deposits

To fund your Kalshi wallet, send tokens directly to the `kalshiWalletAddress` returned from [Create User](/trading-api/users/create-user).

| Token | Chain | Address | Purpose |
|-------|-------|---------|---------|
| USDC | Solana (mainnet-beta) | `EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v` | Trading funds |
| SOL | Solana (mainnet-beta) | Native token | Transaction fees |

<Warning>
You must send both USDC and SOL to trade on Kalshi. USDC is used for trading, while SOL is required to pay Solana transaction fees.
</Warning>

<Note>
To withdraw unused SOL, use the [Withdraw SOL](/trading-api/funds/withdraw-sol) endpoint.
</Note>

---

## Polymarket Deposits (Bridge API)

Returns deposit addresses for funding a Polymarket wallet from other chains. Supports deposits from Ethereum, Solana, Bitcoin, and other networks. Deposits are automatically bridged to USDC.e on Polygon.

<ParamField query="wallet" type="string" required>
  The Polymarket wallet address (0x...) from [Create User](/trading-api/users/create-user).
</ParamField>

<RequestExample>
```bash cURL
curl -H "x-api-key: YOUR_API_KEY" \
  "https://trade.predexon.com/api/bridge/deposit?wallet=0x1234567890abcdef1234567890abcdef12345678"
```

```python Python
import requests

wallet_address = "0x1234567890abcdef1234567890abcdef12345678"

response = requests.get(
    "https://trade.predexon.com/api/bridge/deposit",
    headers={"x-api-key": "YOUR_API_KEY"},
    params={"wallet": wallet_address}
)

deposit_info = response.json()

print("Deposit addresses:")
print(f"  EVM chains: {deposit_info['depositAddresses']['evm']}")
print(f"  Solana: {deposit_info['depositAddresses']['solana']}")
print(f"  Bitcoin: {deposit_info['depositAddresses']['bitcoin']}")
```

```typescript TypeScript
const walletAddress = "0x1234567890abcdef1234567890abcdef12345678";

const response = await fetch(
  `https://trade.predexon.com/api/bridge/deposit?wallet=${walletAddress}`,
  { headers: { "x-api-key": "YOUR_API_KEY" } }
);

const depositInfo = await response.json();
```
</RequestExample>

<ResponseField name="wallet" type="string">
  The queried wallet address.
</ResponseField>

<ResponseField name="depositAddresses" type="object">
  Deposit addresses for different chain types.
  <Expandable title="Deposit addresses">
    <ResponseField name="evm" type="string">
      Address for EVM chain deposits (Ethereum, Arbitrum, Base, etc.)
    </ResponseField>
    <ResponseField name="solana" type="string">
      Address for Solana deposits
    </ResponseField>
    <ResponseField name="bitcoin" type="string">
      Address for Bitcoin deposits
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="supportedAssets" type="array">
  List of supported tokens and chains.
  <Expandable title="Asset object">
    <ResponseField name="chain" type="string">
      Chain name (e.g., "Ethereum", "Solana", "Bitcoin")
    </ResponseField>
    <ResponseField name="chainId" type="string">
      Chain identifier
    </ResponseField>
    <ResponseField name="token" type="string">
      Token symbol (e.g., "USDC", "ETH", "SOL")
    </ResponseField>
    <ResponseField name="tokenAddress" type="string">
      Token contract address
    </ResponseField>
    <ResponseField name="decimals" type="number">
      Token decimal places
    </ResponseField>
    <ResponseField name="minDepositUsd" type="number">
      Minimum deposit amount in USD
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseExample>
```json 200 OK
{
  "wallet": "0x1234567890abcdef1234567890abcdef12345678",
  "depositAddresses": {
    "evm": "0xabcd...1234",
    "solana": "7BfknMUrXjZ96swfM4uLzDXi9QgtcdjQKiWcG4QxW2So",
    "bitcoin": "bc1q..."
  },
  "supportedAssets": [
    {
      "chain": "Ethereum",
      "chainId": "1",
      "token": "USDC",
      "tokenAddress": "0xa0b86991c6218b36c1d19d4a2e9eb0ce3606eb48",
      "decimals": 6,
      "minDepositUsd": 5
    },
    {
      "chain": "Ethereum",
      "chainId": "1",
      "token": "ETH",
      "tokenAddress": "0x0000000000000000000000000000000000000000",
      "decimals": 18,
      "minDepositUsd": 5
    },
    {
      "chain": "Solana",
      "chainId": "solana",
      "token": "USDC",
      "tokenAddress": "EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v",
      "decimals": 6,
      "minDepositUsd": 5
    },
    {
      "chain": "Bitcoin",
      "chainId": "bitcoin",
      "token": "BTC",
      "tokenAddress": "",
      "decimals": 8,
      "minDepositUsd": 5
    }
  ]
}
```

```json 400 Bad Request
{
  "error": "Missing required parameter: wallet"
}
```

```json 400 Invalid Address
{
  "error": "Invalid wallet address format"
}
```

```json 500 Bridge Error
{
  "error": "Failed to get deposit info"
}
```
</ResponseExample>

## How It Works

<Steps>
  <Step title="Get deposit addresses">
    Call this endpoint with your Polymarket wallet address.
  </Step>
  <Step title="Send funds">
    Send supported tokens to the appropriate deposit address for your source chain.
  </Step>
  <Step title="Automatic bridging">
    Funds are automatically bridged to USDC.e on Polygon and credited to your wallet.
  </Step>
  <Step title="Start trading">
    Check your balance and begin placing orders.
  </Step>
</Steps>

## Supported Chains

| Source Chain | Deposit Address | Supported Tokens |
|--------------|-----------------|------------------|
| Ethereum | `depositAddresses.evm` | USDC, ETH, USDT |
| Arbitrum | `depositAddresses.evm` | USDC, ETH |
| Base | `depositAddresses.evm` | USDC, ETH |
| Solana | `depositAddresses.solana` | USDC, SOL |
| Bitcoin | `depositAddresses.bitcoin` | BTC |

<Note>
Deposit addresses are cached for 15 minutes. The same address can be reused for multiple deposits.
</Note>

<Warning>
Always check `minDepositUsd` before sending. Deposits below the minimum may not be processed.
</Warning>
