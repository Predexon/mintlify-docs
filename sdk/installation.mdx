---
title: "Installation"
description: "Install the Predexon Trade SDK and set up your project"
---

## Install

```bash
npm install @predexon/trade-sdk
```

## Requirements

| Requirement | Version |
|-------------|---------|
| Node.js | 18+ (20+ recommended for cleanest install) |
| TypeScript | 5.0+ (if using TypeScript) |

## Peer Dependencies

The SDK bundles its dependencies internally. No peer dependencies required.

## Module Formats

The SDK ships as dual ESM/CJS:

<CodeGroup>
```typescript ESM (recommended)
import { PredexonSDK } from '@predexon/trade-sdk';
```

```javascript CommonJS
const { PredexonSDK } = require('@predexon/trade-sdk');
```
</CodeGroup>

## Verify Installation

```typescript
import { PredexonSDK } from '@predexon/trade-sdk';

const sdk = new PredexonSDK({
  apiKey: 'your-api-key',
});

console.log('SDK initialized successfully');
```

## Next Steps

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/sdk/quickstart">
    Place your first order
  </Card>
  <Card title="Configuration" icon="gear" href="/sdk/configuration">
    Configure SDK options
  </Card>
</CardGroup>
